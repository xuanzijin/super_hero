spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://${DB_HOST}:${DB_PORT}/super_hero?characterEncoding=utf8&useSSL=false
      username: ${DB_USER}
      password: ${DB_PWD}
